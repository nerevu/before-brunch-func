{
  "name": "before-brunch-func",
  "version": "0.0.5",
  "description": "A plugin to run a function before every Brunch compile.",
  "main": "./lib/index",
  "author": "Nerevu Labs",
  "license": "MIT",
  "scripts": {
    "prepare": "rm -rf lib && coffee --bare --output lib/ src/",
    "watch": "coffee --bare --watch --output lib/ src/"
  },
  "readmeFilename": "README.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/nerevu/before-brunch-func"
  },
  "keywords": [
    "brunch",
    "before",
    "commands",
    "pre-compile"
  ],
  "dependencies": {
    "lodash.debounce": "^4.0.8",
    "lodash.partial": "^4.2.1"
  },
  "devDependencies": {
    "coffeescript": "^2.3.2",
    "debug": "^4.1.1"
  }
}
